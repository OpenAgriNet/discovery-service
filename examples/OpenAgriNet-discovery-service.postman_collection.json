{
  "info": {
    "name": "OpenAgriNet discovery-service",
    "description": "Beckn v2.0.0 publish and discover against a local stack.\n\n  make run                      # brings up PostgreSQL + the service\n  newman run examples/OpenAgriNet-discovery-service.postman_collection.json\n\nRun the Publish folder first: every discover request asserts the EXACT set of resource ids that the published catalog should produce. Exact rather than 'contains', because a filter that has quietly stopped filtering still returns rows and a subset assertion passes for it.\n\nGENERATED by examples/build-postman.py from the example .json files - edit those and regenerate rather than editing here.\n\nX-Beckn-Degraded: semantic on the text-search responses is expected. The semantic embedding provider defaults to `noop`, so the service declares the mode missing and answers with the modes it does have, rather than failing the request.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8080",
      "type": "string"
    }
  ],
  "item": [
    {
      "name": "Health",
      "item": [
        {
          "name": "GET /healthz",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/healthz",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "healthz"
              ]
            },
            "description": "Answers that the process is up. There is no Compose healthcheck on the service because the runtime stage is distroless/static and has no shell to run one."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /readyz",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/readyz",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "readyz"
              ]
            },
            "description": "Answers that PostgreSQL is reachable."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Publish",
      "item": [
        {
          "name": "01 Publish - Karnataka weather advisory catalog",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/publish",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "publish"
              ]
            },
            "description": "One catalog, one provider, three resources and two offers, built on the OpenAgriNet WeatherAdvisoryCapability schema. Run this FIRST - every discover request below asserts against exactly this data.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"context\": {\n    \"domain\": \"agriculture\",\n    \"action\": \"publish\",\n    \"version\": \"2.0.0\",\n    \"bppId\": \"weather.karnataka.example.org\",\n    \"bppUri\": \"https://weather.karnataka.example.org/beckn\",\n    \"transactionId\": \"3f9a1c62-4d5e-4a7b-9c8d-1e2f3a4b5c6d\",\n    \"messageId\": \"8b7c6d5e-4f3a-42b1-a0c9-d8e7f6a5b4c3\",\n    \"timestamp\": \"2026-08-27T06:30:00Z\",\n    \"schemaContext\": [\n      \"https://schemas.openagrinet.global/schema/WeatherAdvisoryCapability/v0.1/context.jsonld#openagrinet:WeatherAdvisoryCapability\"\n    ]\n  },\n  \"message\": {\n    \"catalogs\": [\n      {\n        \"id\": \"cat-ksndmc-weather-advisory\",\n        \"bppId\": \"weather.karnataka.example.org\",\n        \"bppUri\": \"https://weather.karnataka.example.org/beckn\",\n        \"isActive\": true,\n        \"descriptor\": {\n          \"code\": \"KSNDMC-WX-ADVISORY\",\n          \"name\": \"Karnataka Weather Advisory Services\",\n          \"shortDesc\": \"Weather-informed agricultural advisories for Karnataka\",\n          \"longDesc\": \"Village- and point-level weather advisories covering irrigation, spraying and crop protection decisions across Karnataka.\"\n        },\n        \"provider\": {\n          \"id\": \"prov-ksndmc\",\n          \"descriptor\": {\n            \"code\": \"KSNDMC\",\n            \"name\": \"Karnataka State Natural Disaster Monitoring Centre\"\n          },\n          \"availableAt\": [\n            {\n              \"geo\": {\n                \"type\": \"Polygon\",\n                \"coordinates\": [\n                  [\n                    [\n                      74.05,\n                      11.55\n                    ],\n                    [\n                      78.6,\n                      11.55\n                    ],\n                    [\n                      78.6,\n                      18.45\n                    ],\n                    [\n                      74.05,\n                      18.45\n                    ],\n                    [\n                      74.05,\n                      11.55\n                    ]\n                  ]\n                ]\n              }\n            }\n          ]\n        },\n        \"validity\": {\n          \"startDate\": \"2026-01-01T00:00:00Z\",\n          \"endDate\": \"2027-12-31T23:59:59Z\"\n        },\n        \"resources\": [\n          {\n            \"id\": \"res-wx-village-belagavi\",\n            \"descriptor\": {\n              \"code\": \"WX-VILLAGE-BLG\",\n              \"name\": \"Village weather advisory, Belagavi\",\n              \"shortDesc\": \"Seven-day village-level irrigation and spraying advisory\",\n              \"longDesc\": \"Rainfall, temperature, humidity and wind advisories at village granularity for the Belagavi district, refreshed every three hours.\"\n            },\n            \"resourceAttributes\": {\n              \"@context\": \"https://schemas.openagrinet.global/schema/WeatherAdvisoryCapability/v0.1/context.jsonld\",\n              \"@type\": \"openagrinet:WeatherAdvisoryCapability\",\n              \"subjectCategories\": [\n                \"Crop\",\n                \"Weather\"\n              ],\n              \"agricultureSubjects\": [\n                {\n                  \"subjectId\": \"https://taxonomy.openagrinet.global/crop/sugarcane\",\n                  \"subjectType\": \"Crop\",\n                  \"descriptor\": {\n                    \"code\": \"SUGARCANE\",\n                    \"name\": \"Sugarcane\"\n                  }\n                }\n              ],\n              \"languages\": [\n                \"en\",\n                \"hi\",\n                \"kn\",\n                \"mr\"\n              ],\n              \"coverageAreas\": [\n                {\n                  \"codeScheme\": \"ISO-3166-2\",\n                  \"areaCode\": \"IN-KA\",\n                  \"areaLevel\": \"State\",\n                  \"areaName\": \"Karnataka\"\n                },\n                {\n                  \"type\": \"Polygon\",\n                  \"coordinates\": [\n                    [\n                      [\n                        74.05,\n                        15.45\n                      ],\n                      [\n                        75.25,\n                        15.45\n                      ],\n                      [\n                        75.25,\n                        16.55\n                      ],\n                      [\n                        74.05,\n                        16.55\n                      ],\n                      [\n                        74.05,\n                        15.45\n                      ]\n                    ]\n                  ]\n                }\n              ],\n              \"topics\": [\n                \"Irrigation\",\n                \"Spraying\",\n                \"CropProtection\"\n              ],\n              \"weatherParameters\": [\n                \"Rainfall\",\n                \"Temperature\",\n                \"Humidity\",\n                \"WindSpeed\"\n              ],\n              \"forecastHorizon\": \"P7D\",\n              \"updateFrequency\": \"PT3H\",\n              \"geographicGranularity\": \"Village\"\n            }\n          },\n          {\n            \"id\": \"res-wx-point-dharwad\",\n            \"descriptor\": {\n              \"code\": \"WX-POINT-DWD\",\n              \"name\": \"Point weather advisory, Dharwad\",\n              \"shortDesc\": \"Three-day hyperlocal spraying advisory\",\n              \"longDesc\": \"Hourly-refreshed rainfall, wind speed and humidity advisory for a single field location near Dharwad, intended for spray-window decisions.\"\n            },\n            \"resourceAttributes\": {\n              \"@context\": \"https://schemas.openagrinet.global/schema/WeatherAdvisoryCapability/v0.1/context.jsonld\",\n              \"@type\": \"openagrinet:WeatherAdvisoryCapability\",\n              \"subjectCategories\": [\n                \"Crop\",\n                \"Weather\"\n              ],\n              \"agricultureSubjects\": [\n                {\n                  \"subjectId\": \"https://taxonomy.openagrinet.global/crop/cotton\",\n                  \"subjectType\": \"Crop\",\n                  \"descriptor\": {\n                    \"code\": \"COTTON\",\n                    \"name\": \"Cotton\"\n                  }\n                }\n              ],\n              \"languages\": [\n                \"en\",\n                \"kn\"\n              ],\n              \"coverageAreas\": [\n                {\n                  \"type\": \"Point\",\n                  \"coordinates\": [\n                    75.0078,\n                    15.4589\n                  ]\n                }\n              ],\n              \"topics\": [\n                \"Spraying\",\n                \"Irrigation\"\n              ],\n              \"weatherParameters\": [\n                \"Rainfall\",\n                \"WindSpeed\",\n                \"Humidity\"\n              ],\n              \"forecastHorizon\": \"P3D\",\n              \"updateFrequency\": \"PT1H\",\n              \"geographicGranularity\": \"Point\"\n            }\n          },\n          {\n            \"id\": \"res-wx-alert-statewide\",\n            \"descriptor\": {\n              \"code\": \"WX-ALERT-KA\",\n              \"name\": \"Statewide severe weather alerts\",\n              \"shortDesc\": \"District-level cyclone and heavy rainfall alerts\",\n              \"longDesc\": \"Severe weather alerting at district granularity for the whole of Karnataka. Carries no coordinate geometry, only a governed area code.\"\n            },\n            \"resourceAttributes\": {\n              \"@context\": \"https://schemas.openagrinet.global/schema/WeatherAdvisoryCapability/v0.1/context.jsonld\",\n              \"@type\": \"openagrinet:WeatherAdvisoryCapability\",\n              \"subjectCategories\": [\n                \"Weather\"\n              ],\n              \"languages\": [\n                \"en\",\n                \"kn\"\n              ],\n              \"coverageAreas\": [\n                {\n                  \"codeScheme\": \"ISO-3166-2\",\n                  \"areaCode\": \"IN-KA\",\n                  \"areaLevel\": \"State\",\n                  \"areaName\": \"Karnataka\"\n                }\n              ],\n              \"topics\": [\n                \"CropProtection\"\n              ],\n              \"weatherParameters\": [\n                \"Alert\",\n                \"Rainfall\",\n                \"WindSpeed\"\n              ],\n              \"forecastHorizon\": \"P2D\",\n              \"updateFrequency\": \"PT30M\",\n              \"geographicGranularity\": \"District\"\n            }\n          }\n        ],\n        \"offers\": [\n          {\n            \"id\": \"offer-wx-free-tier\",\n            \"resourceIds\": [\n              \"res-wx-village-belagavi\",\n              \"res-wx-alert-statewide\"\n            ],\n            \"descriptor\": {\n              \"code\": \"FREE-TIER\",\n              \"name\": \"Free public advisory tier\"\n            }\n          },\n          {\n            \"id\": \"offer-wx-subscription\",\n            \"resourceIds\": [\n              \"res-wx-point-dharwad\"\n            ],\n            \"descriptor\": {\n              \"code\": \"SUBSCRIPTION\",\n              \"name\": \"Paid hyperlocal subscription\"\n            }\n          }\n        ]\n      }\n    ],\n    \"publishDirectives\": [\n      {\n        \"catalogId\": \"cat-ksndmc-weather-advisory\",\n        \"catalogType\": \"REGULAR\",\n        \"visibleTo\": [\n          \"local-network\"\n        ]\n      }\n    ]\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "const req = JSON.parse(pm.request.body.raw);",
                  "",
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"callback action is catalog/on_publish\", () =>",
                  "    pm.expect(res.context.action).to.eql(\"catalog/on_publish\"));",
                  "",
                  "pm.test(\"transactionId and messageId are echoed\", () => {",
                  "    pm.expect(res.context.transactionId).to.eql(req.context.transactionId);",
                  "    pm.expect(res.context.messageId).to.eql(req.context.messageId);",
                  "});",
                  "",
                  "pm.test(\"not a NACK\", () =>",
                  "    pm.expect(res.message.status, JSON.stringify(res.message.error))",
                  "      .to.not.eql(\"NACK\"));",
                  "",
                  "pm.test(\"the catalog was ACCEPTED with no errors\", () => {",
                  "    const result = res.message.results[0];",
                  "    pm.expect(result.catalogId).to.eql(\"cat-ksndmc-weather-advisory\");",
                  "    pm.expect(result.status, JSON.stringify(result.errors)).to.eql(\"ACCEPTED\");",
                  "});",
                  "",
                  "pm.test(\"three resources and one provider were indexed\", () => {",
                  "    pm.expect(res.message.results[0].stats.itemCount).to.eql(3);",
                  "    pm.expect(res.message.results[0].stats.providerCount).to.eql(1);",
                  "});",
                  "",
                  "// Publish is idempotent under MERGE, so running this collection twice is safe",
                  "// and the second run asserts that too."
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Discover",
      "item": [
        {
          "name": "02 Text search - irrigation spraying advisory",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/discover",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "discover"
              ]
            },
            "description": "The statewide alert says 'Severe weather alerting' and must NOT match. It is the control: if the lexical index were being skipped, all three would come back.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"context\": {\n    \"domain\": \"agriculture\",\n    \"action\": \"discover\",\n    \"version\": \"2.0.0\",\n    \"transactionId\": \"a1000000-0000-4000-8000-000000000002\",\n    \"messageId\": \"b1000000-0000-4000-8000-000000000002\",\n    \"timestamp\": \"2026-08-27T06:30:00Z\"\n  },\n  \"message\": {\n    \"intent\": {\n      \"textSearch\": \"irrigation spraying advisory\"\n    }\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "const req = JSON.parse(pm.request.body.raw);",
                  "",
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"callback action is on_discover\", () =>",
                  "    pm.expect(res.context.action).to.eql(\"on_discover\"));",
                  "",
                  "pm.test(\"version is 2.0.0\", () =>",
                  "    pm.expect(res.context.version).to.eql(\"2.0.0\"));",
                  "",
                  "pm.test(\"transactionId and messageId are echoed\", () => {",
                  "    pm.expect(res.context.transactionId).to.eql(req.context.transactionId);",
                  "    pm.expect(res.context.messageId).to.eql(req.context.messageId);",
                  "});",
                  "",
                  "// A NACK arrives with its own shape and would otherwise slip past every",
                  "// assertion above that only looks at `catalogs`.",
                  "pm.test(\"not a NACK\", () =>",
                  "    pm.expect(res.message.status, JSON.stringify(res.message.error))",
                  "      .to.not.eql(\"NACK\"));",
                  "",
                  "const resourceIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.resources || []).map(r => r.id).sort();",
                  "",
                  "// EXACT, not \"contains\": a filter that has quietly stopped filtering still",
                  "// returns rows, and a subset assertion passes for it.",
                  "pm.test(\"resources are exactly \" + JSON.stringify([\"res-wx-point-dharwad\", \"res-wx-village-belagavi\"]), () =>",
                  "    pm.expect(resourceIds).to.eql([\"res-wx-point-dharwad\", \"res-wx-village-belagavi\"]));",
                  "",
                  "// The service declares a missing retrieval mode rather than failing: the",
                  "// semantic mode defaults to `noop`, so this header is EXPECTED locally.",
                  "const degraded = pm.response.headers.get(\"X-Beckn-Degraded\");",
                  "if (degraded) { console.log(\"degraded modes: \" + degraded); }"
                ]
              }
            }
          ]
        },
        {
          "name": "09 Text search - OR over terms, not AND",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/discover",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "discover"
              ]
            },
            "description": "'irrigation' appears only in the village and point resources; 'cyclone' only in the statewide alert. All three coming back is what proves lexical retrieval ORs its terms - under AND none would match. Case 02 cannot tell the two apart, so without this one the semantics were only assumed.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"context\": {\n    \"domain\": \"agriculture\",\n    \"action\": \"discover\",\n    \"version\": \"2.0.0\",\n    \"transactionId\": \"a1000000-0000-4000-8000-000000000009\",\n    \"messageId\": \"b1000000-0000-4000-8000-000000000009\",\n    \"timestamp\": \"2026-08-27T06:30:00Z\"\n  },\n  \"message\": {\n    \"intent\": {\n      \"textSearch\": \"irrigation cyclone\"\n    }\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "const req = JSON.parse(pm.request.body.raw);",
                  "",
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"callback action is on_discover\", () =>",
                  "    pm.expect(res.context.action).to.eql(\"on_discover\"));",
                  "",
                  "pm.test(\"version is 2.0.0\", () =>",
                  "    pm.expect(res.context.version).to.eql(\"2.0.0\"));",
                  "",
                  "pm.test(\"transactionId and messageId are echoed\", () => {",
                  "    pm.expect(res.context.transactionId).to.eql(req.context.transactionId);",
                  "    pm.expect(res.context.messageId).to.eql(req.context.messageId);",
                  "});",
                  "",
                  "// A NACK arrives with its own shape and would otherwise slip past every",
                  "// assertion above that only looks at `catalogs`.",
                  "pm.test(\"not a NACK\", () =>",
                  "    pm.expect(res.message.status, JSON.stringify(res.message.error))",
                  "      .to.not.eql(\"NACK\"));",
                  "",
                  "const resourceIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.resources || []).map(r => r.id).sort();",
                  "",
                  "// EXACT, not \"contains\": a filter that has quietly stopped filtering still",
                  "// returns rows, and a subset assertion passes for it.",
                  "pm.test(\"resources are exactly \" + JSON.stringify([\"res-wx-alert-statewide\", \"res-wx-point-dharwad\", \"res-wx-village-belagavi\"]), () =>",
                  "    pm.expect(resourceIds).to.eql([\"res-wx-alert-statewide\", \"res-wx-point-dharwad\", \"res-wx-village-belagavi\"]));",
                  "",
                  "// The service declares a missing retrieval mode rather than failing: the",
                  "// semantic mode defaults to `noop`, so this header is EXPECTED locally.",
                  "const degraded = pm.response.headers.get(\"X-Beckn-Degraded\");",
                  "if (degraded) { console.log(\"degraded modes: \" + degraded); }"
                ]
              }
            }
          ]
        },
        {
          "name": "03 schemaContext - WeatherAdvisoryCapability",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/discover",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "discover"
              ]
            },
            "description": "schemaContext is a CONTEXT field, not an intent one - Intent is additionalProperties:false, so sending it under message.intent is a body that fails its own schema. All three resources carry the same @context, so this case pins acceptance rather than discrimination.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"context\": {\n    \"domain\": \"agriculture\",\n    \"action\": \"discover\",\n    \"version\": \"2.0.0\",\n    \"transactionId\": \"a1000000-0000-4000-8000-000000000003\",\n    \"messageId\": \"b1000000-0000-4000-8000-000000000003\",\n    \"timestamp\": \"2026-08-27T06:30:00Z\",\n    \"schemaContext\": [\n      \"https://schemas.openagrinet.global/schema/WeatherAdvisoryCapability/v0.1/context.jsonld#openagrinet:WeatherAdvisoryCapability\"\n    ]\n  },\n  \"message\": {\n    \"intent\": {\n      \"textSearch\": \"weather\"\n    }\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "const req = JSON.parse(pm.request.body.raw);",
                  "",
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"callback action is on_discover\", () =>",
                  "    pm.expect(res.context.action).to.eql(\"on_discover\"));",
                  "",
                  "pm.test(\"version is 2.0.0\", () =>",
                  "    pm.expect(res.context.version).to.eql(\"2.0.0\"));",
                  "",
                  "pm.test(\"transactionId and messageId are echoed\", () => {",
                  "    pm.expect(res.context.transactionId).to.eql(req.context.transactionId);",
                  "    pm.expect(res.context.messageId).to.eql(req.context.messageId);",
                  "});",
                  "",
                  "// A NACK arrives with its own shape and would otherwise slip past every",
                  "// assertion above that only looks at `catalogs`.",
                  "pm.test(\"not a NACK\", () =>",
                  "    pm.expect(res.message.status, JSON.stringify(res.message.error))",
                  "      .to.not.eql(\"NACK\"));",
                  "",
                  "const resourceIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.resources || []).map(r => r.id).sort();",
                  "",
                  "// EXACT, not \"contains\": a filter that has quietly stopped filtering still",
                  "// returns rows, and a subset assertion passes for it.",
                  "pm.test(\"resources are exactly \" + JSON.stringify([\"res-wx-alert-statewide\", \"res-wx-point-dharwad\", \"res-wx-village-belagavi\"]), () =>",
                  "    pm.expect(resourceIds).to.eql([\"res-wx-alert-statewide\", \"res-wx-point-dharwad\", \"res-wx-village-belagavi\"]));",
                  "",
                  "// The service declares a missing retrieval mode rather than failing: the",
                  "// semantic mode defaults to `noop`, so this header is EXPECTED locally.",
                  "const degraded = pm.response.headers.get(\"X-Beckn-Degraded\");",
                  "if (degraded) { console.log(\"degraded modes: \" + degraded); }"
                ]
              }
            }
          ]
        },
        {
          "name": "04 Spatial - S_DWITHIN 25km of Dharwad",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/discover",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "discover"
              ]
            },
            "description": "The Dharwad Point is ~2km away and the Belagavi Polygon contains the query coordinate. The statewide alert carries only an ISO-3166-2 area CODE and no coordinates, so nothing was cell-indexed for it and it is not spatially discoverable at all - by design.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"context\": {\n    \"domain\": \"agriculture\",\n    \"action\": \"discover\",\n    \"version\": \"2.0.0\",\n    \"transactionId\": \"a1000000-0000-4000-8000-000000000004\",\n    \"messageId\": \"b1000000-0000-4000-8000-000000000004\",\n    \"timestamp\": \"2026-08-27T06:30:00Z\"\n  },\n  \"message\": {\n    \"intent\": {\n      \"spatial\": [\n        {\n          \"op\": \"S_DWITHIN\",\n          \"targets\": \"$.catalogs[*].resources[*].resourceAttributes.coverageAreas[*]\",\n          \"geometry\": {\n            \"type\": \"Point\",\n            \"coordinates\": [\n              75.02,\n              15.47\n            ]\n          },\n          \"distanceMeters\": 25000\n        }\n      ]\n    }\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "const req = JSON.parse(pm.request.body.raw);",
                  "",
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"callback action is on_discover\", () =>",
                  "    pm.expect(res.context.action).to.eql(\"on_discover\"));",
                  "",
                  "pm.test(\"version is 2.0.0\", () =>",
                  "    pm.expect(res.context.version).to.eql(\"2.0.0\"));",
                  "",
                  "pm.test(\"transactionId and messageId are echoed\", () => {",
                  "    pm.expect(res.context.transactionId).to.eql(req.context.transactionId);",
                  "    pm.expect(res.context.messageId).to.eql(req.context.messageId);",
                  "});",
                  "",
                  "// A NACK arrives with its own shape and would otherwise slip past every",
                  "// assertion above that only looks at `catalogs`.",
                  "pm.test(\"not a NACK\", () =>",
                  "    pm.expect(res.message.status, JSON.stringify(res.message.error))",
                  "      .to.not.eql(\"NACK\"));",
                  "",
                  "const resourceIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.resources || []).map(r => r.id).sort();",
                  "",
                  "// EXACT, not \"contains\": a filter that has quietly stopped filtering still",
                  "// returns rows, and a subset assertion passes for it.",
                  "pm.test(\"resources are exactly \" + JSON.stringify([\"res-wx-point-dharwad\", \"res-wx-village-belagavi\"]), () =>",
                  "    pm.expect(resourceIds).to.eql([\"res-wx-point-dharwad\", \"res-wx-village-belagavi\"]));",
                  "",
                  "// The service declares a missing retrieval mode rather than failing: the",
                  "// semantic mode defaults to `noop`, so this header is EXPECTED locally.",
                  "const degraded = pm.response.headers.get(\"X-Beckn-Degraded\");",
                  "if (degraded) { console.log(\"degraded modes: \" + degraded); }"
                ]
              }
            }
          ]
        },
        {
          "name": "05 Spatial - S_INTERSECTS inside Belagavi only",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/discover",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "discover"
              ]
            },
            "description": "(74.50, 16.00) is deep inside the Belagavi polygon and ~120km from the Dharwad point. This case and case 04 have to DISAGREE - that is what separates a working predicate from one matching the whole catalog.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"context\": {\n    \"domain\": \"agriculture\",\n    \"action\": \"discover\",\n    \"version\": \"2.0.0\",\n    \"transactionId\": \"a1000000-0000-4000-8000-000000000005\",\n    \"messageId\": \"b1000000-0000-4000-8000-000000000005\",\n    \"timestamp\": \"2026-08-27T06:30:00Z\"\n  },\n  \"message\": {\n    \"intent\": {\n      \"spatial\": [\n        {\n          \"op\": \"S_INTERSECTS\",\n          \"targets\": \"$['catalogs'][*]['resources'][*]['resourceAttributes']['coverageAreas'][*]\",\n          \"geometry\": {\n            \"type\": \"Point\",\n            \"coordinates\": [\n              74.5,\n              16.0\n            ]\n          }\n        }\n      ]\n    }\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "const req = JSON.parse(pm.request.body.raw);",
                  "",
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"callback action is on_discover\", () =>",
                  "    pm.expect(res.context.action).to.eql(\"on_discover\"));",
                  "",
                  "pm.test(\"version is 2.0.0\", () =>",
                  "    pm.expect(res.context.version).to.eql(\"2.0.0\"));",
                  "",
                  "pm.test(\"transactionId and messageId are echoed\", () => {",
                  "    pm.expect(res.context.transactionId).to.eql(req.context.transactionId);",
                  "    pm.expect(res.context.messageId).to.eql(req.context.messageId);",
                  "});",
                  "",
                  "// A NACK arrives with its own shape and would otherwise slip past every",
                  "// assertion above that only looks at `catalogs`.",
                  "pm.test(\"not a NACK\", () =>",
                  "    pm.expect(res.message.status, JSON.stringify(res.message.error))",
                  "      .to.not.eql(\"NACK\"));",
                  "",
                  "const resourceIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.resources || []).map(r => r.id).sort();",
                  "",
                  "// EXACT, not \"contains\": a filter that has quietly stopped filtering still",
                  "// returns rows, and a subset assertion passes for it.",
                  "pm.test(\"resources are exactly \" + JSON.stringify([\"res-wx-village-belagavi\"]), () =>",
                  "    pm.expect(resourceIds).to.eql([\"res-wx-village-belagavi\"]));",
                  "",
                  "const offerIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.offers || []).map(o => o.id).sort();",
                  "",
                  "pm.test(\"offers are exactly \" + JSON.stringify([\"offer-wx-free-tier\"]), () =>",
                  "    pm.expect(offerIds).to.eql([\"offer-wx-free-tier\"]));",
                  "",
                  "// The service declares a missing retrieval mode rather than failing: the",
                  "// semantic mode defaults to `noop`, so this header is EXPECTED locally.",
                  "const degraded = pm.response.headers.get(\"X-Beckn-Degraded\");",
                  "if (degraded) { console.log(\"degraded modes: \" + degraded); }"
                ]
              }
            }
          ]
        },
        {
          "name": "06 Filter - geographicGranularity == Village",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/discover",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "discover"
              ]
            },
            "description": "A jsonpath filter rooted at the resource level.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"context\": {\n    \"domain\": \"agriculture\",\n    \"action\": \"discover\",\n    \"version\": \"2.0.0\",\n    \"transactionId\": \"a1000000-0000-4000-8000-000000000006\",\n    \"messageId\": \"b1000000-0000-4000-8000-000000000006\",\n    \"timestamp\": \"2026-08-27T06:30:00Z\"\n  },\n  \"message\": {\n    \"intent\": {\n      \"textSearch\": \"weather advisory\",\n      \"filters\": {\n        \"type\": \"jsonpath\",\n        \"expression\": \"$.catalogs[*].resources[*] ? (@.resourceAttributes.geographicGranularity == \\\"Village\\\")\"\n      }\n    }\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "const req = JSON.parse(pm.request.body.raw);",
                  "",
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"callback action is on_discover\", () =>",
                  "    pm.expect(res.context.action).to.eql(\"on_discover\"));",
                  "",
                  "pm.test(\"version is 2.0.0\", () =>",
                  "    pm.expect(res.context.version).to.eql(\"2.0.0\"));",
                  "",
                  "pm.test(\"transactionId and messageId are echoed\", () => {",
                  "    pm.expect(res.context.transactionId).to.eql(req.context.transactionId);",
                  "    pm.expect(res.context.messageId).to.eql(req.context.messageId);",
                  "});",
                  "",
                  "// A NACK arrives with its own shape and would otherwise slip past every",
                  "// assertion above that only looks at `catalogs`.",
                  "pm.test(\"not a NACK\", () =>",
                  "    pm.expect(res.message.status, JSON.stringify(res.message.error))",
                  "      .to.not.eql(\"NACK\"));",
                  "",
                  "const resourceIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.resources || []).map(r => r.id).sort();",
                  "",
                  "// EXACT, not \"contains\": a filter that has quietly stopped filtering still",
                  "// returns rows, and a subset assertion passes for it.",
                  "pm.test(\"resources are exactly \" + JSON.stringify([\"res-wx-village-belagavi\"]), () =>",
                  "    pm.expect(resourceIds).to.eql([\"res-wx-village-belagavi\"]));",
                  "",
                  "const offerIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.offers || []).map(o => o.id).sort();",
                  "",
                  "pm.test(\"offers are exactly \" + JSON.stringify([\"offer-wx-free-tier\"]), () =>",
                  "    pm.expect(offerIds).to.eql([\"offer-wx-free-tier\"]));",
                  "",
                  "// The service declares a missing retrieval mode rather than failing: the",
                  "// semantic mode defaults to `noop`, so this header is EXPECTED locally.",
                  "const degraded = pm.response.headers.get(\"X-Beckn-Degraded\");",
                  "if (degraded) { console.log(\"degraded modes: \" + degraded); }"
                ]
              }
            }
          ]
        },
        {
          "name": "07 Filter - cross-level, offer predicate selects a resource",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/discover",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "discover"
              ]
            },
            "description": "Rooted at the OFFER level and yet it narrows RESOURCES. This exercises the single composite filter_doc column (A18): under the earlier three-column design an offer-rooted predicate could not select a resource at all. If this returns all three, the composite regressed.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"context\": {\n    \"domain\": \"agriculture\",\n    \"action\": \"discover\",\n    \"version\": \"2.0.0\",\n    \"transactionId\": \"a1000000-0000-4000-8000-000000000007\",\n    \"messageId\": \"b1000000-0000-4000-8000-000000000007\",\n    \"timestamp\": \"2026-08-27T06:30:00Z\"\n  },\n  \"message\": {\n    \"intent\": {\n      \"textSearch\": \"weather\",\n      \"filters\": {\n        \"type\": \"jsonpath\",\n        \"expression\": \"$.catalogs[*].offers[*] ? (@.descriptor.code == \\\"SUBSCRIPTION\\\")\"\n      }\n    }\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "const req = JSON.parse(pm.request.body.raw);",
                  "",
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"callback action is on_discover\", () =>",
                  "    pm.expect(res.context.action).to.eql(\"on_discover\"));",
                  "",
                  "pm.test(\"version is 2.0.0\", () =>",
                  "    pm.expect(res.context.version).to.eql(\"2.0.0\"));",
                  "",
                  "pm.test(\"transactionId and messageId are echoed\", () => {",
                  "    pm.expect(res.context.transactionId).to.eql(req.context.transactionId);",
                  "    pm.expect(res.context.messageId).to.eql(req.context.messageId);",
                  "});",
                  "",
                  "// A NACK arrives with its own shape and would otherwise slip past every",
                  "// assertion above that only looks at `catalogs`.",
                  "pm.test(\"not a NACK\", () =>",
                  "    pm.expect(res.message.status, JSON.stringify(res.message.error))",
                  "      .to.not.eql(\"NACK\"));",
                  "",
                  "const resourceIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.resources || []).map(r => r.id).sort();",
                  "",
                  "// EXACT, not \"contains\": a filter that has quietly stopped filtering still",
                  "// returns rows, and a subset assertion passes for it.",
                  "pm.test(\"resources are exactly \" + JSON.stringify([\"res-wx-point-dharwad\"]), () =>",
                  "    pm.expect(resourceIds).to.eql([\"res-wx-point-dharwad\"]));",
                  "",
                  "const offerIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.offers || []).map(o => o.id).sort();",
                  "",
                  "pm.test(\"offers are exactly \" + JSON.stringify([\"offer-wx-subscription\"]), () =>",
                  "    pm.expect(offerIds).to.eql([\"offer-wx-subscription\"]));",
                  "",
                  "// The service declares a missing retrieval mode rather than failing: the",
                  "// semantic mode defaults to `noop`, so this header is EXPECTED locally.",
                  "const degraded = pm.response.headers.get(\"X-Beckn-Degraded\");",
                  "if (degraded) { console.log(\"degraded modes: \" + degraded); }"
                ]
              }
            }
          ]
        },
        {
          "name": "10 Combination - text AND geo",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/discover",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "discover"
              ]
            },
            "description": "Constraints INTERSECT. 'cotton' appears only in the point resource and 'cyclone' only in the alert, so text alone gives point+alert; the 25km circle alone gives point+village. Only the point is in both. Each dimension excludes something the other admits, which is what makes a dropped predicate visible here.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"context\": {\n    \"domain\": \"agriculture\",\n    \"action\": \"discover\",\n    \"version\": \"2.0.0\",\n    \"transactionId\": \"a1000000-0000-4000-8000-000000000010\",\n    \"messageId\": \"b1000000-0000-4000-8000-000000000010\",\n    \"timestamp\": \"2026-08-27T06:30:00Z\"\n  },\n  \"message\": {\n    \"intent\": {\n      \"textSearch\": \"cotton cyclone\",\n      \"spatial\": [\n        {\n          \"op\": \"S_DWITHIN\",\n          \"targets\": \"$.catalogs[*].resources[*].resourceAttributes.coverageAreas[*]\",\n          \"geometry\": {\n            \"type\": \"Point\",\n            \"coordinates\": [\n              75.02,\n              15.47\n            ]\n          },\n          \"distanceMeters\": 25000\n        }\n      ]\n    }\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "const req = JSON.parse(pm.request.body.raw);",
                  "",
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"callback action is on_discover\", () =>",
                  "    pm.expect(res.context.action).to.eql(\"on_discover\"));",
                  "",
                  "pm.test(\"version is 2.0.0\", () =>",
                  "    pm.expect(res.context.version).to.eql(\"2.0.0\"));",
                  "",
                  "pm.test(\"transactionId and messageId are echoed\", () => {",
                  "    pm.expect(res.context.transactionId).to.eql(req.context.transactionId);",
                  "    pm.expect(res.context.messageId).to.eql(req.context.messageId);",
                  "});",
                  "",
                  "// A NACK arrives with its own shape and would otherwise slip past every",
                  "// assertion above that only looks at `catalogs`.",
                  "pm.test(\"not a NACK\", () =>",
                  "    pm.expect(res.message.status, JSON.stringify(res.message.error))",
                  "      .to.not.eql(\"NACK\"));",
                  "",
                  "const resourceIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.resources || []).map(r => r.id).sort();",
                  "",
                  "// EXACT, not \"contains\": a filter that has quietly stopped filtering still",
                  "// returns rows, and a subset assertion passes for it.",
                  "pm.test(\"resources are exactly \" + JSON.stringify([\"res-wx-point-dharwad\"]), () =>",
                  "    pm.expect(resourceIds).to.eql([\"res-wx-point-dharwad\"]));",
                  "",
                  "// The service declares a missing retrieval mode rather than failing: the",
                  "// semantic mode defaults to `noop`, so this header is EXPECTED locally.",
                  "const degraded = pm.response.headers.get(\"X-Beckn-Degraded\");",
                  "if (degraded) { console.log(\"degraded modes: \" + degraded); }"
                ]
              }
            }
          ]
        },
        {
          "name": "11 Combination - geo AND attribute filter",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/discover",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "discover"
              ]
            },
            "description": "Geo alone gives point+village, the FREE-TIER offer covers village+alert. Village is the only overlap. Note there is no textSearch at all: a NULL query_text must admit every row the other predicates allow, not none, or a geo-only discover answers empty.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"context\": {\n    \"domain\": \"agriculture\",\n    \"action\": \"discover\",\n    \"version\": \"2.0.0\",\n    \"transactionId\": \"a1000000-0000-4000-8000-000000000011\",\n    \"messageId\": \"b1000000-0000-4000-8000-000000000011\",\n    \"timestamp\": \"2026-08-27T06:30:00Z\"\n  },\n  \"message\": {\n    \"intent\": {\n      \"spatial\": [\n        {\n          \"op\": \"S_DWITHIN\",\n          \"targets\": \"$.catalogs[*].resources[*].resourceAttributes.coverageAreas[*]\",\n          \"geometry\": {\n            \"type\": \"Point\",\n            \"coordinates\": [\n              75.02,\n              15.47\n            ]\n          },\n          \"distanceMeters\": 25000\n        }\n      ],\n      \"filters\": {\n        \"type\": \"jsonpath\",\n        \"expression\": \"$.catalogs[*].offers[*] ? (@.descriptor.code == \\\"FREE-TIER\\\")\"\n      }\n    }\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "const req = JSON.parse(pm.request.body.raw);",
                  "",
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"callback action is on_discover\", () =>",
                  "    pm.expect(res.context.action).to.eql(\"on_discover\"));",
                  "",
                  "pm.test(\"version is 2.0.0\", () =>",
                  "    pm.expect(res.context.version).to.eql(\"2.0.0\"));",
                  "",
                  "pm.test(\"transactionId and messageId are echoed\", () => {",
                  "    pm.expect(res.context.transactionId).to.eql(req.context.transactionId);",
                  "    pm.expect(res.context.messageId).to.eql(req.context.messageId);",
                  "});",
                  "",
                  "// A NACK arrives with its own shape and would otherwise slip past every",
                  "// assertion above that only looks at `catalogs`.",
                  "pm.test(\"not a NACK\", () =>",
                  "    pm.expect(res.message.status, JSON.stringify(res.message.error))",
                  "      .to.not.eql(\"NACK\"));",
                  "",
                  "const resourceIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.resources || []).map(r => r.id).sort();",
                  "",
                  "// EXACT, not \"contains\": a filter that has quietly stopped filtering still",
                  "// returns rows, and a subset assertion passes for it.",
                  "pm.test(\"resources are exactly \" + JSON.stringify([\"res-wx-village-belagavi\"]), () =>",
                  "    pm.expect(resourceIds).to.eql([\"res-wx-village-belagavi\"]));",
                  "",
                  "const offerIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.offers || []).map(o => o.id).sort();",
                  "",
                  "pm.test(\"offers are exactly \" + JSON.stringify([\"offer-wx-free-tier\"]), () =>",
                  "    pm.expect(offerIds).to.eql([\"offer-wx-free-tier\"]));",
                  "",
                  "// The service declares a missing retrieval mode rather than failing: the",
                  "// semantic mode defaults to `noop`, so this header is EXPECTED locally.",
                  "const degraded = pm.response.headers.get(\"X-Beckn-Degraded\");",
                  "if (degraded) { console.log(\"degraded modes: \" + degraded); }"
                ]
              }
            }
          ]
        },
        {
          "name": "12 Combination - all three, disjoint, expects EMPTY",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/discover",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "discover"
              ]
            },
            "description": "Text selects the point resource, the geometry selects the village, and no resource is both. Empty is the only answer a conjunction can give. If the dimensions were OR'd this returns two resources; if one were dropped it returns one. Cases 10 and 11 are what stop this passing vacuously against a service returning nothing for everything.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"context\": {\n    \"domain\": \"agriculture\",\n    \"action\": \"discover\",\n    \"version\": \"2.0.0\",\n    \"transactionId\": \"a1000000-0000-4000-8000-000000000012\",\n    \"messageId\": \"b1000000-0000-4000-8000-000000000012\",\n    \"timestamp\": \"2026-08-27T06:30:00Z\"\n  },\n  \"message\": {\n    \"intent\": {\n      \"textSearch\": \"cotton\",\n      \"spatial\": [\n        {\n          \"op\": \"S_INTERSECTS\",\n          \"targets\": \"$.catalogs[*].resources[*].resourceAttributes.coverageAreas[*]\",\n          \"geometry\": {\n            \"type\": \"Point\",\n            \"coordinates\": [\n              74.5,\n              16.0\n            ]\n          }\n        }\n      ],\n      \"filters\": {\n        \"type\": \"jsonpath\",\n        \"expression\": \"$.catalogs[*].offers[*] ? (@.descriptor.code == \\\"FREE-TIER\\\")\"\n      }\n    }\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "const req = JSON.parse(pm.request.body.raw);",
                  "",
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"callback action is on_discover\", () =>",
                  "    pm.expect(res.context.action).to.eql(\"on_discover\"));",
                  "",
                  "pm.test(\"version is 2.0.0\", () =>",
                  "    pm.expect(res.context.version).to.eql(\"2.0.0\"));",
                  "",
                  "pm.test(\"transactionId and messageId are echoed\", () => {",
                  "    pm.expect(res.context.transactionId).to.eql(req.context.transactionId);",
                  "    pm.expect(res.context.messageId).to.eql(req.context.messageId);",
                  "});",
                  "",
                  "// A NACK arrives with its own shape and would otherwise slip past every",
                  "// assertion above that only looks at `catalogs`.",
                  "pm.test(\"not a NACK\", () =>",
                  "    pm.expect(res.message.status, JSON.stringify(res.message.error))",
                  "      .to.not.eql(\"NACK\"));",
                  "",
                  "const resourceIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.resources || []).map(r => r.id).sort();",
                  "",
                  "// EXACT, not \"contains\": a filter that has quietly stopped filtering still",
                  "// returns rows, and a subset assertion passes for it.",
                  "pm.test(\"resources are exactly \" + JSON.stringify([]), () =>",
                  "    pm.expect(resourceIds).to.eql([]));",
                  "",
                  "// The service declares a missing retrieval mode rather than failing: the",
                  "// semantic mode defaults to `noop`, so this header is EXPECTED locally.",
                  "const degraded = pm.response.headers.get(\"X-Beckn-Degraded\");",
                  "if (degraded) { console.log(\"degraded modes: \" + degraded); }"
                ]
              }
            }
          ]
        },
        {
          "name": "13 Retrieval modes UNION - misspelled query, trigram not tsvector",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/discover",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "discover"
              ]
            },
            "description": "Every term is misspelled, so the tsvector admits nothing. The resource comes back through the trigram index on `name`, which gates on similarity >= 0.3 (pg_trgm's default threshold) and NOT on the searchable text the lexical mode uses. Constraints intersect, but retrieval modes union - this is the case that tells them apart.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"context\": {\n    \"domain\": \"agriculture\",\n    \"action\": \"discover\",\n    \"version\": \"2.0.0\",\n    \"transactionId\": \"a1000000-0000-4000-8000-000000000013\",\n    \"messageId\": \"b1000000-0000-4000-8000-000000000013\",\n    \"timestamp\": \"2026-08-27T06:30:00Z\"\n  },\n  \"message\": {\n    \"intent\": {\n      \"textSearch\": \"Vilage weathr advisery Belgavi\"\n    }\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "const req = JSON.parse(pm.request.body.raw);",
                  "",
                  "pm.test(\"HTTP 200\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"callback action is on_discover\", () =>",
                  "    pm.expect(res.context.action).to.eql(\"on_discover\"));",
                  "",
                  "pm.test(\"version is 2.0.0\", () =>",
                  "    pm.expect(res.context.version).to.eql(\"2.0.0\"));",
                  "",
                  "pm.test(\"transactionId and messageId are echoed\", () => {",
                  "    pm.expect(res.context.transactionId).to.eql(req.context.transactionId);",
                  "    pm.expect(res.context.messageId).to.eql(req.context.messageId);",
                  "});",
                  "",
                  "// A NACK arrives with its own shape and would otherwise slip past every",
                  "// assertion above that only looks at `catalogs`.",
                  "pm.test(\"not a NACK\", () =>",
                  "    pm.expect(res.message.status, JSON.stringify(res.message.error))",
                  "      .to.not.eql(\"NACK\"));",
                  "",
                  "const resourceIds = (res.message.catalogs || [])",
                  "    .flatMap(c => c.resources || []).map(r => r.id).sort();",
                  "",
                  "// EXACT, not \"contains\": a filter that has quietly stopped filtering still",
                  "// returns rows, and a subset assertion passes for it.",
                  "pm.test(\"resources are exactly \" + JSON.stringify([\"res-wx-village-belagavi\"]), () =>",
                  "    pm.expect(resourceIds).to.eql([\"res-wx-village-belagavi\"]));",
                  "",
                  "// The service declares a missing retrieval mode rather than failing: the",
                  "// semantic mode defaults to `noop`, so this header is EXPECTED locally.",
                  "const degraded = pm.response.headers.get(\"X-Beckn-Degraded\");",
                  "if (degraded) { console.log(\"degraded modes: \" + degraded); }"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Refusals",
      "item": [
        {
          "name": "08 Refusal - jsonpath with no ?(...) filter",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/discover",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "discover"
              ]
            },
            "description": "Expected to FAIL with 400. See the test script for why this shape is dangerous enough to refuse.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"context\": {\n    \"domain\": \"agriculture\",\n    \"action\": \"discover\",\n    \"version\": \"2.0.0\",\n    \"transactionId\": \"a1000000-0000-4000-8000-000000000008\",\n    \"messageId\": \"b1000000-0000-4000-8000-000000000008\",\n    \"timestamp\": \"2026-08-27T06:30:00Z\"\n  },\n  \"message\": {\n    \"intent\": {\n      \"textSearch\": \"weather\",\n      \"filters\": {\n        \"type\": \"jsonpath\",\n        \"expression\": \"$.catalogs[*].resources[*].resourceAttributes.geographicGranularity == \\\"Village\\\"\"\n      }\n    }\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "",
                  "// The same intent as case 06 written WITHOUT the ?(...) filter. PostgreSQL",
                  "// runs it happily: `@?` is given a comparison, a comparison always yields an",
                  "// item, and `false` is an item - so it answers true for EVERY row and the",
                  "// caller receives the whole corpus formatted as a filtered page, with no",
                  "// error anywhere. A 400 here is the feature, not a limitation.",
                  "pm.test(\"HTTP 400\", () => pm.response.to.have.status(400));",
                  "",
                  "pm.test(\"refused as SCH_INVALID_JSONPATH\", () =>",
                  "    pm.expect(res.message.error.code).to.eql(\"SCH_INVALID_JSONPATH\"));",
                  "",
                  "pm.test(\"the fault names the expression\", () =>",
                  "    pm.expect(res.message.error.details.path)",
                  "      .to.eql(\"$.message.intent.filters.expression\"));"
                ]
              }
            }
          ]
        }
      ]
    }
  ]
}
